# ── LangChain + Groq (LLM calls inside agents) ───────────────────
langchain>=0.3.0              # Core LangChain — chains, prompts, parsers
langchain-core>=0.3.0         # Base classes (Runnable, etc.)
langchain-groq>=0.2.0         # LangChain wrapper for Groq API
langsmith>=0.1.120            # LLM monitoring and tracing dashboard

# ── Groq API ─────────────────────────────────────────────────────
groq>=0.9.0                   # Groq SDK (used by langchain-groq internally)

# ── FastAPI server ───────────────────────────────────────────────
fastapi>=0.115.0              # REST API
uvicorn[standard]>=0.30.0     # Runs the FastAPI server

# ── MCP server (Claude Code tool integration) ────────────────────
mcp[cli]>=1.3.0               # FastMCP server

# ── Email ────────────────────────────────────────────────────────
imap-tools>=1.7.0             # Read emails via IMAP

# ── PDF text extraction ──────────────────────────────────────────
pdfplumber>=0.11.0            # Primary PDF reader
pypdf>=4.3.0                  # Fallback PDF reader

# ── Utilities ────────────────────────────────────────────────────
python-dotenv>=1.0.0          # Load .env file
pydantic>=2.10.0              # Data validation
rich>=13.9.0                  # Pretty terminal output
